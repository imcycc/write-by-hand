

class Scanner {
  constructor(string) {
    this.string = string
    this.tail = string
    this.pos = 0
  }

  // 返回匹配文本，指针移动到匹配文本之后，（匹配文本必须在开头）
  scan(re) {

  }

  // 返回匹配文本之前的文本，指针移动到匹配文本之前
  scanUtil(re) {

  }
}

export default new Scanner()